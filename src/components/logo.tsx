import { Mail } from 'lucide-react';

export default function Logo() {
  return (
    <div className="flex items-center justify-center size-14 bg-primary rounded-2xl text-primary-foreground">
      <Mail className="size-8" />
    </div>
  );
}
